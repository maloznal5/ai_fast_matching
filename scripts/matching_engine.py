def process_client_request(raw_text, client_name="Гість"):
    print(f"🤖 ШІ аналізує запит: '{raw_text}'")
    
    # Імітація пошуку по базі 20к позицій (Етап 2)
    # Дані базуються на ваших файлах: заявка.pdf та Додаток 3
    catalog = {
        "болт 14х100": {"sku": "933-14-100", "name": "Болт М14х100 DIN 933 8.8", "weight": 0.145},
        "гайка 16": {"sku": "934-16", "name": "Гайка М16 DIN 934 8.0", "weight": 0.033}
    }
    
    query = raw_text.lower()
    for key, data in catalog.items():
        if key in query:
            print(f"✅ Знайдено відповідність на krepezh.ua: {data['name']}")
            print(f"⚖️ Розрахунок ваги: 1000 шт = {data['weight']*1000} кг")
            return data
    
    print("❌ Точної відповідності не знайдено. Потрібна консультація ШІ.")

if __name__ == "__main__":
    process_client_request("Мені потрібно болт 14х100 для тендеру")
